*** Preliminary testing for injection pass on fibers *** 
- Pass executed with inject and inline settings, injecting every 50 bitcode instructions
- Test executed: 'fibertime1'
- Cycles per Iteration: ~8-10 (wrapper_nk_fiber_yield : running)
- Inject script --- Clang commands NOT edited

